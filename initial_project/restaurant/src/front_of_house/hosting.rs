/// Add person to wait list
///
/// # Examples
///
/// ```
/// let past = len(waitlist);
/// restaurant::add_to_waitlist(person);
///
/// assert_eq!(past + 1, len(waitlist));
/// ```
pub fn add_to_waitlist() {
    println!("I am add_to_waitlist");
}